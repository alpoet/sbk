默认数据库用户是root 密码为空
部署步骤：
1.将www文件夹内所有内容拷贝到服务器的www文件夹内，将sbkCore文件夹拷贝到www的上层目录中，这是为了安全考虑。
2.登录phpMyAdmin，新建一个名为sbk的数据库，导入sbk.sql，默认的用户为root密码为空，可在sbkCore/Conf/config.php中配置您的mysql用户名和密码
3.访问浏览器的地址栏127.0.0.1，应该就可以使用了，在这里说明一下，由于本系统是HTML5写的，请使用类似谷歌浏览器这种安全先进的浏览器访问会获得更加良好的用户体验，IE和360这样的浏览器既落后也不安全，如果支持国产用遨游也不错。

	